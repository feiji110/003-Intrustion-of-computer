一存储：
1.数组：（元素与元素之间在内存中连续存储，需要一整块连续的内存）
【挨着】顺序存储（唯一头与尾）除首元素外，都有前驱
方便查找（p+字节*索引），不方便插入删除
2.链式：（比数组需要的内存大的多，但不要求连续）节点包括：
每个元素的节点部分都要存储下一个
（1）数据：
（2）指针：[指向后节点内存地址】
访问元素会先访问之前的元素（与数组比代价大）
约瑟夫环
二：逻辑
1.栈：死胡同(后进先出）【逆置】进制转换（从下向上）
========】（迷宫求解）
2.队列：（先进先出）秒杀系统

